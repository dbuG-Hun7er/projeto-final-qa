{
  "name": "api-tests",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "test": "mocha \"tests/**/*.spec.js\" --reporter spec"
  },
  "devDependencies": {
    "chai": "^4.4.1",
    "mocha": "^10.0.0",
    "supertest": "^7.0.0"
  }
}
